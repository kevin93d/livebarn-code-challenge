import styled from 'styled-components';
import { Paper } from '@material-ui/core';

const Wrapper = styled(Paper)`
  padding-left: 32px; // button 12 px
  padding-right: 20px;
  display: flex;
`;

export { Wrapper };
